/**
 * Reserved Vite entry for the Projectauto workflow builder.
 *
 * The current builder is rendered inline from the Blade page so it remains
 * usable even when module assets have not been built yet. This entry keeps the
 * module asset pipeline deterministic and provides a stable place for future
 * extraction of the builder UI into compiled assets.
 */

export default {};
